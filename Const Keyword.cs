//using const in front of the variable's data type means that the variable can't be overwrite and its value
//is unchangeable

const int num=10;
num =20; //this line will cause error
Console.WriteLine(num);
